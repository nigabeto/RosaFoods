@model PizzaListViewModel

<div>
    <h2>@Model.CategoriaAtual</h2>
    @* <h2>@ViewData["Titulo"]</h2> *@
</div>
@* <h2>@ViewData["Data"]</h2>
<hr />
<h3>@ViewBag.Total @ViewBag.TotalPizzas</h3>
<hr />

<h1>@TempData["Nome"]</h1> *@ @* Nigabeto *@

<div class="container">
    <div class="row">
        @foreach (var pizza in Model.Pizzas)
        {
            <partial name="_PizzasResumo" for="@pizza"/>
        }
    </div>
</div>
